# Forward Literary Packet Instruction

Return exactly one literary payload conforming to the supplied frozen protocol
definitions. Use only assigned technical-slot join keys. Do not generate alternatives, critiques, replacement
passages, or unplanned candidates.

Write exactly the supplied one-scene chunk and its assigned artifacts/formal
compositions at the intersection of immutable past constraints and active future
obligations. Stay within the supplied `literary_word_allowance`, which is this scene's
share of its logical packet's 4,800-word allowance and includes nested artifact prose.
Make the scene perform at least two dramatic jobs, make evidence
change interpretation, preserve meaningful state variation, and keep choices
concrete and consequential. Use supplied threshold geomancy as symbolic weather
without recomputing it.

Immutable prior prose and facts are evidence. You may read, quote, react to,
reinterpret perspectivally, contradict diegetically, and create consequences from
them. You may not rewrite, improve, regenerate, silently retcon, or ignore them. When
derived constraint state conflicts with an original passage, the original passage wins. If
prior history is awkward but executable, live with it.

Character dialogue and inference may rely only on the supplied relevant knowledge,
belief, suspicion, and claim state. Declare tracked reliance in
`epistemic_dependencies`. Active obligations create gravity, not dictated prose;
advance or satisfy them only when dramatically earned. Future planning may adapt
after this packet commits, but this packet may not edit that plan or earlier canon.

Do not emit constraint metadata. After this artistic payload is schema-valid, a
separate temperature-zero normalization pass derives minimal facts, knowledge,
motifs, soft pressures, and updates to existing obligations without changing prose.

Nest choices, artifacts, and formal compositions under their owning scene. Every choice uses `destination`. Use `kind: technical_slot` for content that is
not yet committed, with an ID from the frozen architecture. Use `kind: content` only
for a literary ID already committed or authored in this packet. Never invent the
future literary ID of an unwritten scene or ending. The deterministic projector later
resolves technical slots without changing this packet.

Use the active web composition fields for navigational purpose, document behavior,
path trace, and state-sensitive presentation. Do not write visual-novel staging or
assume runtime generation. Avoid generic ominous prose, faux profundity, lore dumping, therapist-speak,
exposition-vending characters, arbitrary abstraction, cosmetic variants, inert
atmosphere, and decorative artifacts. The historical `prose_mdx` and `body_mdx` fields
contain constrained Markdown only: no HTML, JSX, imports, exports, or executable MDX.
Return complete, safe Markdown source.
A valid response is preserved even if later normalization must pause. Invalid output receives bounded correction attempts and then pauses the run.
Account for the supplied constraints. Do not merely remember or summarize them.

FROZEN AND RETRIEVED CONTEXT BEGINS

{{PACKET_CONTEXT}}

FROZEN AND RETRIEVED CONTEXT ENDS

EXACT PACKET OBLIGATIONS BEGIN

{{PACKET_OBLIGATIONS}}

EXACT PACKET OBLIGATIONS END

RELEVANT PROTOCOL DEFINITIONS BEGIN

{{PACKET_PROTOCOL_DEFINITIONS}}

RELEVANT PROTOCOL DEFINITIONS END

Return the one-scene literary packet chunk now.
