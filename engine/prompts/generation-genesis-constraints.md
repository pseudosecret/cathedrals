# Genesis Constraint Instruction

Return exactly one `genesis_constraints` record conforming to the supplied schema.
Return JSON only.

Extract only minimal initial constraints grounded in the immutable foundation and cast:
at most 12 nonduplicate canonical facts, 24 knowledge changes, and 8 motif events.
Every source must resolve to committed Genesis material. Leave new obligations,
obligation updates, and foreclosure candidates empty; Architecture owns initial future
pressure. Do not add fiction, revise wording, repeat equivalent facts, or emit a
synopsis. This valid record commits without revision.

IMMUTABLE GENESIS CONTEXT BEGINS

{{GENESIS_CONSTRAINT_CONTEXT}}

IMMUTABLE GENESIS CONTEXT ENDS

RELEVANT PROTOCOL DEFINITIONS BEGIN

{{GENESIS_CONSTRAINT_PROTOCOL_DEFINITIONS}}

RELEVANT PROTOCOL DEFINITIONS END

Return the one Genesis constraint record now.
