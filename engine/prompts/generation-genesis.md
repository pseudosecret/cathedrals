# Genesis Creative Instruction

Use only after preflight has frozen the run manifest. Return exactly one `genesis`
record conforming to `engine/data/generation-protocol.schema.json`; no Markdown fence,
commentary, candidate alternatives, placeholders, or omitted content.

You are authoring the generated root canon for one autonomous Cathedrals run. Generate
all five claimants simultaneously from the five semantically blank technical slots.
Generate significant non-claimant characters, the exact threshold incident, core
chronology, relationships, governing motifs, environmental mutation law, principal
tensions, and source-located initial canonical facts, character knowledge, and motif
pressure. Canonical facts must be minimal facts that constrain lawful future writing,
not a synopsis. World truth and character knowledge/belief/claim are distinct.

The active format is web. Establish format composition law for reader-perspective
literary exploration, documentary evidence, navigational discovery, and deterministic
stateful choices. Generate one `web_art_direction` interpreting the frozen genre/flavor
and root canon through only the bounded protocol dimensions. It must describe
presentation rather than introduce fictional facts, contain no CSS or JavaScript, and
use a palette whose text and muted colors remain readable against its backgrounds.
The deterministic projector owns all frontend code. Do not invent visual-novel staging
or defer content to runtime.

Do not infer claimant archetype, occupation, voice, evidence family, genre,
contradiction mode, or consequence from slot order. Obey `claimants.yaml` as
differentiation law. Keep the exact fiction inside the snowbound hospice-annex event
class and Cathedrals ontology without merely restating that envelope.

This valid response will commit without aesthetic candidate selection. After commit,
every identity, fact, relationship, motif, and wording in it is immutable. There is no
rewrite or regeneration. Weak but valid work survives; malformed output kills the run.
Emit the complete `constraint_delta`; leave obligation and foreclosure arrays empty
because architecture creates the initial future constraint graph.

FROZEN RUN AND ENGINE CONTEXT BEGINS

{{FROZEN_GENESIS_CONTEXT}}

FROZEN RUN AND ENGINE CONTEXT ENDS

RELEVANT PROTOCOL DEFINITIONS BEGIN

{{GENESIS_PROTOCOL_DEFINITIONS}}

RELEVANT PROTOCOL DEFINITIONS END

Return the one genesis record now.
