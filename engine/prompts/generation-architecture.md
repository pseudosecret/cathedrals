# Architecture Creative Instruction

Return exactly one `architecture` record conforming to the supplied frozen protocol
definitions. Do not return commentary, candidates, or prose for scenes not requested
by this record.

Using immutable engine canon and committed genesis, create the full macroarchitecture:
arc purposes, macro branch topology, exact three-option first hub, downstream splits,
stateful reconvergence, unequal depth, major decisions and hesitation, hidden-route
opportunities, narrative promises, ending families, redirective possibilities, and a
dependency-valid traversal plan for every literary and ending packet.

Architecture is creative generated-work canon, not disposable planning. Freeze hard
narrative obligations, promises, branch pressures, technical scene/ending slots, and
packet dependencies without unnecessarily prewriting how unwritten scenes implement
them. Put adaptable future possibilities in `soft_guidance`. Assign every technical
slot to exactly one packet. Literary packets contain three to eight scenes; ending
packets contain two to six endings and depend on every path they interpret. Follow the
frozen traversal strategy and supplied geomantic slots. Allocate exact structural
artifact and formal-composition counts to packets so their totals equal the scope
commitment. Plan no more scenes than the user's possible-scene scope and stop below it
only with a real saturation rationale.

Compose for the active web contract. The first live hub follows three to five
introductory scene surfaces and offers exactly three situated actions. All branches,
redirects, variants, and hidden-route opportunities must be generated before play.

This valid response commits as immutable architecture. Later packets must accommodate
it. Do not offer alternatives for selection or reserve a right to revise it.

FROZEN ENGINE AND GENESIS CONTEXT BEGINS

{{FROZEN_ARCHITECTURE_CONTEXT}}

FROZEN ENGINE AND GENESIS CONTEXT ENDS

RELEVANT PROTOCOL DEFINITIONS BEGIN

{{ARCHITECTURE_PROTOCOL_DEFINITIONS}}

RELEVANT PROTOCOL DEFINITIONS END

Return the one architecture record now.
