# Macroarchitecture Core Instruction

Return exactly one Architecture core payload conforming to the supplied schema.
Return JSON only.

Using completed immutable Genesis, author macro arcs, high-level
topology, initial obligation graph, and attractors. This is macro structure, not one
node or obligation per scene. Keep within the schema's fixed bounds; forward packets
may create later obligations. Do not emit packet plans, scenes, prose, alternatives,
or candidates. The engine freezes scope and record metadata. Attractors describe transformations;
later plan batches assign ending slots. The active format is web. This valid payload commits without revision.
Initial obligation termination targets must use obligation or attractor IDs authored
in this record, never packet IDs.

FROZEN ARCHITECTURE CONTEXT BEGINS

{{FROZEN_ARCHITECTURE_CONTEXT}}

FROZEN ARCHITECTURE CONTEXT ENDS

RELEVANT PROTOCOL DEFINITIONS BEGIN

{{ARCHITECTURE_PROTOCOL_DEFINITIONS}}

RELEVANT PROTOCOL DEFINITIONS END

Return the Architecture core payload now.
