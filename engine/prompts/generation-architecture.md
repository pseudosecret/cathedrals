# Architecture Creative Instruction

Return exactly one `architecture` record conforming to the supplied frozen protocol
definitions. Do not return commentary, candidates, or prose for scenes not requested
by this record.

Using immutable engine canon and committed genesis, create major topology, branch
families, exact three-option first hub, stateful reconvergence, unequal depth, ending
attractors, other-scale attractors, and the initial obligation graph.

Freeze structural necessity, not choreography. An ending attractor specifies a
terminal transformation, thematic function, emotional register, hard prerequisites,
soft seed conditions, and substantial unresolved realization. It is not ending prose
or an exact ending scene. Ambitious and strange transformations are welcome when
causally supportable.

Backward-chain each attractor using the reversed Cathedrals grammar:

```text
desired residue
← required consequence
← required choice/deferment
← required pressure
← required interpretation
← required encounter
```

Represent the resulting chain as typed obligations connected by `requires`; do not
author earlier prose. Distinguish universal hard obligations from obligations scoped
to foreclosable attractors. Give every active long-range debt at least one lawful
termination target. Preserve intentional ambiguity through an explicit resolution
mode rather than abandonment.

`packet_plans` are a technical slot envelope and initial traversal possibility set,
not frozen realization order. Dependencies are immutable causal constraints;
`initial_priority`, retrieval tags, and soft guidance seed later calculation. Do not
put tentative obligation assignments in architecture; the prospective plan computes
them after each commit. Each ending packet realizes exactly one
ending attractor, owns exactly that attractor's ending slots, and depends on every
literary packet so final prose remains late. Assign every technical slot once and fit
the frozen scope/count budgets. Put all initial obligations in `obligation_graph` and
leave `constraint_delta.new_obligations` empty.

Technical slot IDs are the only stable names for unwritten future scenes and endings.
Architecture should create enough frozen slots for later packets to link forward
without guessing future generated literary IDs. Creative IDs remain unspecified until
their content is authored.

Compose for the active web contract. The first live hub follows three to five
introductory scene surfaces and offers exactly three situated actions. All branches,
redirects, variants, and hidden-route opportunities must be generated before play.

This valid response commits attractors, topology, obligation graph, and dependencies
as immutable architecture canon. Later prospective schedules may adapt; history and
architecture canon may not. Do not offer candidates for selection.

FROZEN ENGINE AND GENESIS CONTEXT BEGINS

{{FROZEN_ARCHITECTURE_CONTEXT}}

FROZEN ENGINE AND GENESIS CONTEXT ENDS

RELEVANT PROTOCOL DEFINITIONS BEGIN

{{ARCHITECTURE_PROTOCOL_DEFINITIONS}}

RELEVANT PROTOCOL DEFINITIONS END

Return the one architecture record now.
