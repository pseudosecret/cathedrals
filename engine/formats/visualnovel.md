# Deferred Visual-Novel Contract

Visual-novel aliases are accepted at request time but ignored for active generation:
the frozen effective format is currently `web`. Ren'Py, dialogue-heavy composition,
staging, images, music, and SFX remain a future engine revision. This file is not an
active generation contract and must not trigger a Ren'Py download or projection.
